# `src/` is `tree-sitter generate` output, committed because Zed compiles it directly
# rather than generating it. Collapse it in diffs and keep it out of language stats.
editors/tree-sitter-mwl/src/** linguist-generated=true
