# SPDX-License-Identifier: MIT

# Its own workspace on purpose: this builds for wasm32-wasip1, and the compiler's
# workspace builds for the host.
[workspace]

[package]
name = "zed-minewell"
version = "0.1.0"
edition = "2021"
license = "MIT"
publish = false

[lib]
crate-type = ["cdylib"]

[dependencies]
zed_extension_api = "0.6"
