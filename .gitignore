# Anywhere, not just the root: each example under examples/ builds into its own.
target/
**/*.rs.bk
*.pdb

# insta
**/*.snap.new

# build output
/dist

# Zed builds in place, next to the manifest it was told to load: the WASM component
# and a git checkout per grammar.
editors/zed/extension.wasm
editors/zed/grammars/
