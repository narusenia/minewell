# minewell-lang 実装計画

要件定義: [`01-requirements.md`](./01-requirements.md)

---

## 進捗サマリ

| # | マイルストーン | 状態 | 進捗 |
|---|---|---|---|
| M0 | `tinymcf`（インタプリタ） | **完了** | 14 / 14 |
| M1 | Hello World 縦断 | **完了** | 9 / 9 |
| M2 | 値と式 | **完了** | 7 / 7 |
| M3 | 制御フロー | **完了** | 7 / 7 |
| M4 | 関数と再帰 | **完了** | 6 / 6 |
| M5 | 実行コンテキスト ★ | **完了** | 7 / 7 |
| M6 | スキーマ統合 | **完了** | 8 / 8 |
| M7 | 複合型 | **完了** | 9 / 9 |
| M8 | 数値拡張 | **完了** | 7 / 7 |
| M9 | 仕上げ | **完了** | 11 / 11 |
| M10 | 座標とブロック | **完了** | 6 / 6 |
| M11 | ブロック状態・可視性・LSP | **完了** | 3 / 3 |
| — | 横断（CI・文書・エディタ） | **完了** | 7 / 7 |
| | **合計** | | **101 / 101** |

状態は `未着手` / `進行中` / `完了` / `保留`。
タスクを閉じたらチェックボックスと上表の両方を更新する。

---

## 運用ルール

### TDD サイクル

各タスクは **失敗するテストを書いてから実装する。**
タスクに書かれた「先に書くテスト」がそのままレッドの起点になる。

```
1. 「先に書くテスト」を書く      → 落ちることを確認
2. 通る最小の実装を書く          → 通ることを確認
3. リファクタ                    → テストが通り続けることを確認
4. コミット（1 タスク = 1 コミット以上）
```

### ツールとタスク（mise）

ツールチェーンとタスクは [`mise.toml`](../mise.toml) で管理する。

```
mise install              ツール一式（rust + rustfmt/clippy、cargo-insta）
mise run test             全テスト
mise run test:tinymcf     tinymcf のみ（M0 の作業中はこれ）
mise run snap             insta スナップショットのレビュー
mise run lint             clippy（警告はエラー扱い）
mise run fmt              整形
mise run ci               fmt:check + lint + test。push 前にこれ
```

mise は rust を minimal profile で入れるため、`components = "rustfmt,clippy"` の指定が必須。
JDK は M6（`toolchain build-from-jar`）で必要になった時点で `[tools]` に足す。

### 完了条件（DoD）

タスクは次をすべて満たしたときに完了とする。

- 「先に書くテスト」が通る
- `mise run ci` がグリーン
- 生成物を伴う変更は `insta` スナップショットを更新済み（`mise run snap`）

### コミット粒度

- 1 タスク = 1 コミット以上。複数タスクをまとめない
- Conventional prefix 必須（`feat:` / `fix:` / `refactor:` / `test:` / `chore:` / `docs:` / `perf:` / `ci:`）
- **コミットメッセージに本書のタスク ID（`M2-3` 等）を書かない。** 変更内容を書く

### ブランチ

- `feat/<concrete-name>` 形式。タスク名から具体的な名前を作る
  - 良: `feat/tarjan-scc-analysis` / `feat/execute-context-checking`
  - 悪: `feat/m4` / `feat/milestone-4`

---

## M0. `tinymcf` — mcfunction インタプリタ

**目的:** 以降すべての TDD の測定器を作る。コンパイラがゼロの状態で単体完成する。

**このマイルストーンの意義** — 測定器が無い状態で TDD を始めると、M1〜M4 のテストが
全部 golden file になり、M5 以降で必ず書き直しになる。ここを飛ばしてはならない。

**完了の判定:** 手書きの `.mcfunction` で書いた階乗・フィボナッチが正しい値を返す。

**仕様:** [`crates/tinymcf/SPEC.md`](../crates/tinymcf/SPEC.md)。
各タスクは**まず該当節を pending から確定させ、それから実装する。**

- [x] **M0-1** crate 雛形と NBT 値表現
  - `crates/tinymcf/src/nbt.rs`。`Compound` は `BTreeMap`（出力とスナップショットの決定性のため）
  - ルートの workspace `Cargo.toml` を作り、CI の `hashFiles` ガードを外した（X-1）
- [x] **M0-2** 実行状態（World）
  - `world.rs`。未宣言 objective はエラー（0 として黙認しない）、未設定 holder は `None`
- [x] **M0-2b** SNBT のパーサとフォーマッタ
  - `snbt.rs`。`parse(v.to_string()) == v` の往復一致で検証。`:` は bare-word に含めない
- [x] **M0-3** NBT パス解析
  - `path.rs`。解決・書き込み・削除。フィルタは部分一致かつ再帰、タグも一致を要求
  - 書き込み時の中間生成はバニラの preferred parent 規則に合わせた（次の step が添字ならリスト）
- [x] **M0-4** コマンド行の字句解析基盤
  - 引数トークナイザ（語・引用文字列・セレクタ・NBT パス・SNBT 値・貪欲な残り）、
    `Command` 列挙、未対応コマンドの `Unknown` フォールバックとディスパッチ骨格
  - **計画変更:** 全コマンドの構文を先に書き切るのをやめ、M0-5〜M0-10 が
    自分のコマンド族の**構文と実行を同時に**足す。構文だけ先行させると振る舞いで
    検証できず、計画が警告している golden file 化そのものになるため
  - `args.rs` の balanced scanner 1 本で、セレクタ・SNBT・引用文字列を 1 語として扱う
  - `command.rs` は現状 `Unknown` のみ。各コマンド族は実行側のタスクで足す
- [x] **M0-5** `scoreboard` コマンドの実行
  - `objectives add/remove`、`players set/get/add/remove/operation/reset`
  - `operation` の全演算子（`=` `+=` `-=` `*=` `/=` `%=` `<` `>` `><`）
  - `interp.rs` に `Outcome`（success / result）と `Interpreter`。**失敗は中断しない**
  - `/=` `%=` は floor 除算（`-7/2 = -4`、`-7%2 = 1`）。Rust の `/` `%` とは違う
  - 書き込み系は欠損スコアを 0 で作る。`get` は作らず失敗する
- [x] **M0-6** `data` コマンドの実行
  - `get`（数値はスケール倍、文字列は長さ、list/compound は要素数）
  - `modify ... set value / set from / set string <src> <start> <end> / append / prepend / insert / merge`
  - `remove`、`merge`
  - **storage 対象のみ実行。** entity / block は構文解析はするが「モデル化していない」と診断して失敗
  - `data get` は文字列に長さを返す（要件定義 §4.4 の「`String::len()` は無料」の前提を実証）
- [x] **M0-7** `function` 呼び出しと `return`
  - 関数は**ロード時にパース**。空行と `#` コメントを落とす
  - `return` は呼び出し元に伝播しない。末尾まで落ちた場合の result は実行コマンド数
  - `maxCommandChainLength`（既定 65536）を実装。暴走再帰がテストを止めない担保でもある
  - 再帰の停止条件テストは `execute if` が要るので M0-8 に置いた
- [x] **M0-8** `execute` の条件と `store`
  - `if` / `unless`（`score` の比較と `matches`、`data`）
  - `store result` / `store success`（`score` / `storage` へ、型とスケール付き）
  - 受け入れ: `execute if score $a obj matches 1..5 run say hi` が構文解析され実行される
  - `store` は条件が落ちた場合も発火して 0 を書く（要件定義 §13 の `Option<T>` の前提）
  - 未設定スコアは条件を偽にするだけでエラーにしない
- [x] **M0-8b** `execute` のエンティティ系（M5 が要る範囲）
  - `as` / `at` / `positioned` / `in` / `if entity` / `if block` / `if predicate`
  - セレクタ文字列 → ハーネスが登録したエンティティ、の스タブ登録簿と実行コンテキスト
  - **M0-8 から分離した理由:** これらは世界のスタブという別設計を必要とし、
    M0 の受け入れ条件（階乗・フィボナッチ）にも M4 までにも要らない。M5 で要る
  - 先に書くテスト: `as` が位置を変えず、`at` だけが変えること
- [x] **M0-9** マクロ関数
  - `function ns:f {..}` / `with storage`、行頭 `$` のマクロ行、`$(name)` 展開
  - マクロ行はロード時にパースできないので**呼び出しごとにパース**する
  - 引数なしでマクロ関数を呼ぶと失敗 → 要件定義 §10.1「`#[tick]` がマクロ関数化したらエラー」を
    コンパイラ側でテストできる根拠になる
- [x] **M0-10** 副作用コマンドのスタブ
  - `Interpreter::effects` に `(name, args)` を順序付きで記録
  - **実際に走ったコマンドだけ記録される**ので、そのままトレースとして使える
- [x] **M0-11** 計測 API
  - `Interpreter::report()` — 総コマンド数・関数別内訳・最大深さ・予算超過
  - 課金規則を仕様に明記（`execute ... run` は execute 自身と実行先の**両方**に課金）
  - **呼び出し深さ上限（既定 256）を追加。** バニラには無い実装上の制限 —
    tinymcf は再帰で呼び出しを辿るので、上限が無いと暴走再帰が native スタックを溢れさせて
    テストプロセスごと落ちる（実際に落ちた）
- [x] **M0-12** 手書き mcfunction の受け入れテスト
  - `tests/acceptance.rs` + `tests/fixtures/*.mcfunction`。階乗・フィボナッチ・
    list の破壊的反復・マクロ経由の実行時添字・コスト計測
  - フィクスチャは**生成コードと同じ書き方**にしてある。コンパイラが吐く形が
    ここで先に実証されている状態を作るため
  - **M0 完了。** 以降 `tinymcf` は測定器として信頼できる

---

## M1. Hello World 縦断

**目的:** `fn main() { raw!("say hi") }` を全レイヤ貫通させる。以降は「足す」だけになる。

**完了の判定:** `.mwl` → AST → HIR → MIR → `.mcfunction` → `tinymcf` 実行で `say hi` の記録が取れる。

- [x] **M1-1** workspace 構成
  - `crates/tinymcf` / `crates/mwlc` / `crates/mwl`
  - `mwlc/tests/independence.rs` が `[dependencies]` に `tinymcf` が無いことを検査。
    読み取り側が壊れて素通りしないよう、リーダ自体のテストも置いた
- [x] **M1-2** 字句解析
  - 仕様 §2 を全部実装した（最小に留めなかったのは、字句規則が既に確定していて
    後から足すほうが高くつくため）。セレクタ・リソースロケーション・ネストコメント込み
  - **エラーで停止せず収集して継続。** 1 文字の typo が残りのファイルを隠さない
  - `x:i32` はリソースロケーションとして字句解析される（仕様 §2.8 の既知の角）
- [x] **M1-3** 構文解析（最小）
  - `fn` 定義、ブロック、文、マクロ呼び出し、属性。仕様 §3 の範囲ちょうど
  - **エラー回復あり** — アイテム位置では次のアイテム候補まで、ブロック内では文末まで飛ばす。
    1 つの誤りが 1 つの診断で済む
  - マクロ引数はトークン列のまま保持（マクロごとに文法が違うので共通の構文木が無い）
- [x] **M1-4** 診断基盤（`miette`）
  - `Report` = 1 ファイルの問題の集合。`#[related]` で複数を 1 レポートに束ねる
  - 描画は `fancy` feature の中なので **dev-dependency に隔離**。ライブラリ本体は derive のみ
  - 入力末尾のゼロ幅 span（`expected '}'`）も描画できることをテストで固定
- [x] **M1-5** HIR（名前解決のみ）
  - 関数の一意 ID とデータパックパス、名前衝突検出、組み込みマクロの解決
  - **未知の属性をエラーにする。** `#[tik]` が黙って何もしないのは minewell が
    消そうとしている失敗そのもの。未実装の属性は「unknown」ではなく「planned」と言い分ける
  - `mod` はまだ文法に無い。未定義関数呼び出しのテストは呼び出しが入る M4 へ
- [x] **M1-6** MIR（単一基本ブロック）
  - `Inst::Raw` のみ。1 関数 1 ブロック。ブロックグラフが名前どおりになるのは M3
  - 命令は span を保持する（debug ビルドの `# src/foo.mwl:42` 用）
- [x] **M1-7** emit（datapack 出力）
  - **まずメモリ上に pack を組み立て、書き出しは 1 箇所だけ。** スナップショットが
    ファイルシステム無しで取れ、コンパイル失敗が半端な datapack を残さない
  - `data/<ns>/function/` は 1.21 で単数形になった綴り
  - **debug ビルドの `# src/main.mwl:2` を先に入れた。** 後から足すと
    codegen スナップショットが全部書き換わるため（要件定義 §15）
  - `pack_format` は暫定固定値。toolchain 由来になるのは M6
- [x] **M1-8** `mwl build`
  - `driver` が入口側の唯一の I/O、`emit` が出口側の唯一の I/O。間は純粋
  - 構文エラーと名前解決エラーを**1 回のビルドでまとめて**報告する
  - CLI は引数 2 つなので `clap` を入れていない。増えたら入れる
- [x] **M1-9** 縦断統合テスト基盤
  - `crates/mwlc/tests/harness/`。`driver::compile` を純粋関数として切り出し、
    ファイルシステム無しでコンパイル → `tinymcf` にロード → 実行
  - **以降のすべてのマイルストーンがこのヘルパを使う**
  - `tinymcf` は `mwlc` の **dev-dependency**。独立性テストは `[dependencies]` だけを見るので、
    設計どおりの境界がそのまま守られている
  - debug プロファイルで走らせる — 挿入した行番号コメントが実行を変えないことの証明を兼ねる

---

## M2. 値と式

**完了の判定:** `let a = 2 + 3 * 4;` が `tinymcf` 上で 14 を持つ。

- [x] **M2-1** リテラルと `let`、型注釈の構文
  - **M2-1〜M2-6 は 1 コミット。** 式は front end だけ入れても HIR/MIR/emit が
    型を知らずコンパイルが通らない。分けると捨てるスタブを書くことになる
- [x] **M2-2** 型検査（`i32` / `bool`）
  - 型不一致の診断
  - 先に書くテスト: `let x: i32 = true;` が span 付きエラーになること
- [x] **M2-3** 演算子
  - 算術・比較・論理。優先順位は仕様 §3.2、比較は連鎖しない
  - **`&&` / `||` は短絡させず `min` / `max` 1 コマンドに落とした。** M2 の式は
    副作用を持たない（呼び出しが無い）ので観測上区別できない。**M4 で要見直し**（仕様 §6.3）
  - 元の「`false && crash()` で右辺が評価されない」テストは前提が成立しないので削除
- [x] **M2-4** MIR の仮想レジスタと式の平坦化
  - 先に書くテスト: `2 + 3 * 4` が 3 命令に平坦化されること
- [x] **M2-5** レジスタ割り付け（素朴版）
  - テンポラリ名は**プログラム全体で単調増加**。同名が二度と現れないので生存解析なしで正しい
  - ローカルは `$<関数名>.<束縛名>` で修飾（M4 で関数間の衝突を防ぐため）
- [x] **M2-6** scoreboard 命令の emit
  - 1 命令 = 1 コマンド。定数は `set` / `add` / `remove`、定数比較は `matches` レンジ
  - 比較と `!` は**テンポラリを経由せず直接書き込む**（`execute store success` が
    書き込み先を持っているため）
  - **`<ns>:__init` を生成して `minecraft:load` タグに載せた。** objective が無いと
    バニラが全 `scoreboard` コマンドを拒否するので、これは選択肢ではない
  - `#[tick]` / `#[load]` も各タグへ
- [x] **M2-7** 意味論テスト
  - `tests/vertical.rs` の `expressions` モジュール。値とコストの両方を検証
  - `-7 / 2 == -4`、`-7 % 2 == 1`（Rust の `-3` / `-1` とは違う。仕様 §6.2）

---

## M3. 制御フロー

**完了の判定:** `while` と `break` を使ったループが正しい回数で止まる。

- [x] **M3-1** 構文（`if` / `else` / `while` / `loop` / `break` / `continue` / `return`）
  - 条件は `bool` 必須。**`i32` の 0 / 非 0 は条件にできない**（真偽値化を 2 通りにしない）
  - `if` は文であって式。値を持つ `if` は合流点の設計が要るので M4 と一緒に考える
- [x] **M3-2** 制御フローの表現（HIR → MIR）
  - **MIR にジャンプ命令を入れなかった。** ターゲットにジャンプが無いので、
    辺で繋いだ CFG を持つ意味が無い。制御フローは生成関数とガード付き命令の 2 つで表す
  - 「1 命令 = 1 コマンド」は保たれる（ガード付き命令も 1 行）
- [x] **M3-3** インライン / 切り出しの判定
  - 単文で制御が抜けない `if` はインライン。それ以外は関数に切り出す
  - **条件式は `execute if` に直接埋め込む**（`matches` レンジ含む）。
    レジスタに書いてから読み直すと 2 コマンドかかる
- [x] **M3-4** `#[inline]` / `#[no_inline]`
  - `#[inline]` は成立しない場所（`else` 付き・複文・ループ）ではエラー
- [x] **M3-5** ループの lowering
  - ガード + 本体 + 自己末尾再帰の 1 関数。制御が抜けないループの追加コストはゼロ
- [x] **M3-6** `break` / `continue` / `return` の lowering
  - 制御レジスタ `$<fn>.ctl`（0 通常 / 1 break / 2 continue / 3 return）+ 伝播ガード
  - **`continue` を含む本体だけ別関数に切り出す。** インラインのままだと `continue` の
    `return` がループ関数ごと終わらせてしまう
  - **抜けないブロックでは `$ctl` が 1 度も現れない。** break の無いループに
    break の後始末を払わせない
- [x] **M3-7** 生成関数の命名
  - `<親>/if_0` `<親>/else_0` `<親>/while_0` `<親>/while_0/body`
  - `tests/vertical.rs` に `print_generated_commands`（`--ignored`）を用意。
    lowering を目で確認する用

---

## M4. 関数と再帰

**完了の判定:** `.mwl` で書いた `fact(5)` が `tinymcf` 上で 120 を返す。

- [x] **M4-1** 引数と戻り値
  - 引数は**呼び出し先のローカルと同じフェイクプレイヤー**に書く。引数は初期値を
    呼び出し側が入れるローカルにすぎない
  - 戻り値はバニラの関数戻り値をそのまま使う。定数なら `return <n>` 1 コマンド
  - **生成ブロックからの `return` は `$<fn>.ret` + 制御レジスタで運ぶ。**
    mcfunction の `return` は自分が書かれた関数しか抜けないため
  - シグネチャを先に集める 2 パス構成。定義順に依存しない
  - 値を返す関数が末尾に到達しうるならエラー（`if`/`else` 両方が返せば OK）
- [x] **M4-2** 呼び出しグラフの構築
  - HIR を走査して呼び出し辺を集める
- [x] **M4-3** Tarjan による SCC 解析
  - **同じ成分内への呼び出しだけ**が再帰。相互再帰も同じ判定で拾える
- [x] **M4-4** 非再帰関数の割り付け
  - 引数の書き込みと `function` だけ。退避も復帰も出てこない（コスト測定テストで固定）
- [x] **M4-5** 再帰関数のスタックフレーム
  - **呼び出し側で退避する。** 呼び出し先の入口では引数がもう上書きされていて手遅れ
  - 退避対象は「値が入っているローカル + それまでに配ったテンポラリ」。
    `let` の途中で呼ぶ場合、その束縛はまだ空なので退避しない（読むとコマンドが失敗する）
  - 生存解析で絞るのは M9-7
- [x] **M4-6** 意味論テスト
  - `fact(5) == 120`、相互再帰、フレームが空に戻ること、短絡の観測
  - **§6.3 の見直しを実施。** 右辺が呼び出しを含むときだけ分岐して本当に短絡させ、
    純粋なら `min`/`max` 1 コマンドのまま

---

## M5. 実行コンテキスト ★

**目的:** minewell が mcfunction に勝つ最大の理由を実装する。
**ここが動いた時点で「使う価値のある道具」になる。** M6 より優先する。

**完了の判定:** `@s` を要求する関数をコンテキスト無しで呼ぶとコンパイルエラーになる。

- [x] **M5-1** セレクタの字句・構文解析
  - 字句解析器が 1 トークンとして保持（M1-2 で実装済み）。中身はゲームの文法なので触らない
- [x] **M5-2** `Selector` 型（コンパイル時型）
  - `let s = @e[..]` は**コンパイル時の別名**。算術・比較・引数・戻り値に使うとエラー
- [x] **M5-3** `as` / `at` ブロック
  - **`at self` ではなく `at @s`。** `self` は M7 の `impl` レシーバに予約済み（仕様 §3.8）
  - 単文で制御が抜けないブロックはインライン（`execute as <sel> run <cmd>`）
- [x] **M5-4** `for e in sel`
  - `as` のみ適用。位置は動かさない。束縛は本体内で `@s` を意味するコンパイル時別名
  - **`continue` は本体からの `return` そのもの**（本体はエンティティ 1 体につき 1 回）。
    ネストしたブロックからの `continue` だけ制御レジスタ経由で運び、本体先頭で消費する
  - `break` / `return` は本体先頭のガードで「以降のエンティティは即座に戻る」に落ちる。
    `execute as` を途中で止める手段がバニラに無いため。**反復分のコマンドは消えない**
- [x] **M5-5** `#[ctx(...)]` 属性と検査
  - **要求は宣言のみ、推論しない。** 推論だと原因から遠い場所でエラーが出る（仕様 §4.6）
  - `dimension` は入れない。次元を切り替える手段がインタプリタに無く、検査できないため
  - **`#[tick]` / `#[load]` は `#[ctx]` を宣言できない。** function タグは実行者なしで
    呼ぶので、宣言した時点で実行時に黙って何もしないことが確定する
- [x] **M5-6** 診断の質
  - **計画変更:** 要求を宣言制にしたので、不足は常に 1 段で分かる。経路の表示は不要になった
  - 代わりに診断は「何が足りないか」と「`as` で囲む / `#[ctx]` を足す」の両方を示す
- [x] **M5-7** debug ビルドの生存ガード（§6.3）
  - `#[ctx(entity)]` の関数の先頭に `execute unless entity @s run tellraw`。release では出ない
  - コンパイル時に検査できないのは「実行時にエンティティが死んでいる」場合だけなので、
    ガードが見るのもそこだけ

---

## M6. スキーマ統合

**完了の判定:** `commands.json` から生成した型付き API で `setblock` を呼べる。

- [x] **M6-1** `commands.json` ローダ
  - `schema/mod.rs`。**redirect は辿らない**（`/execute run` は root に戻るので止まらない）
  - テストは手書きの小さなフィクスチャ。実物の 1MB を読むテストは遅いだけで何も増やさない
- [x] **M6-2** parser 型 → minewell 型の写像（§1.4）
  - 未知の parser 型は `RawArg` + 警告。**エラーにしない** — スナップショットで引数型が
    1 つ増えただけで toolchain 全体が生成できなくなる
- [x] **M6-3** コマンド API の機械生成
  - コマンドは関数呼び出しの形。**ユーザ定義関数が同名のコマンドを覆い隠す**（唯一のラップ手段）
  - **引数はすべてコンパイル時の値。** 実行時の値を埋めるにはマクロ関数が要る（M9）ので、
    それまでは「まだ実装されていない」と言って止まる。黙って動かないより止まるほうがいい
- [x] **M6-4** `overrides.toml`
  - **リネームのみ。** 機械生成名は literal 経路から作るので、深いコマンドでは
    `data_modify_storage_target_path_set_from_entity` のような誰も打たない名前になる。
    そこを直すのが生成器に出せない唯一の判断
  - シグネチャの上書きは入れていない。手で書き換えられるようにすると、
    コマンド表を人間の管理下に戻すことになり、生成した意味が消える
  - 該当コマンドが無いリネームは**エラーにしない**。1 つのファイルが複数版をまたぐため
- [x] **M6-5** `ResourceLocation` / `Pos` リテラル
  - **後から直した 2 点**（どちらも大きい例を書いて初めて出た）:
    ID のパスに `.` と `-` を含められること（`minecraft:block.note_block.pling` は 1 つの ID）、
    引数の後ろに来るリテラルを正しい位置に書くこと（`playsound <sound> master <targets>`）
  - `minecraft:stone` と `pos!(~ ~1 ~)`。どちらも `Selector` と同じくコンパイル時のみの型
  - 座標の記法混在（`~ ^ ~`）はエラー。registries による ID 検証は M6-6 の toolchain 側
- [x] **M6-6** toolchain の管理
  - `~/.minewell/toolchains/<version>/`（`MINEWELL_HOME` で変更可）。`pack_format` が反映される
  - **`toolchain` は省略できる。** 省略時は暫定 pack_format + コマンド呼び出し不可、`raw!` は使える
  - **コマンド表をコンパイラに埋め込まない。** 埋め込めば「版非依存のコンパイラ」が嘘になる
  - ルートを値として渡す設計（環境変数を関数内で読まない）。テストが並列でも競合しない
- [x] **M6-7** `mwl toolchain list` / `add`
  - **計画変更:** `install`（Releases からの取得）は X-2 が Release を公開してから。
    取得先が存在しないうちに HTTP クライアントを足しても検証できない
  - `build-from-jar` の代わりに `add <version> <commands.json> --pack-format <n>`。
    data generator の実行はコンパイラの外に置く — データパック作者の多くはサーバ運営者ではなく、
    コンパイルに JVM を要求するのは筋が悪い。usage に生成コマンドを書いてある
- [x] **M6-8** `data/` パススルーと ID 存在検査（§13）
  - `data/` を丸ごとコピー。生成物と衝突したら**エラー**（黙ってどちらかを選ばない）
  - **存在しない関数を参照するとコンパイルエラー。** バニラはこれを実行して何も起きず、
    どこにもメッセージが出ない
  - 検査対象は今のところ `minecraft:function` 引数だけ。predicate や loot table の
    parser 名は版で揺れるので、実物の `commands.json` を見てから足す

---

## M7. 複合型

**完了の判定:** `struct` と `enum` を使った状態機械が `tinymcf` 上で動く。

- [x] **M7-1** `struct` の定義と構築
  - 先に書くテスト: フィールド不足の構築がエラーになること
  - 置き場所は `<ns>:mw` の `mw.vars.<関数名>.<束縛名>`（仕様 §6.18）。scoreboard の
    `$<関数名>.<束縛名>` と同じ規則で、理由も同じ（別々の関数の同名ローカルを衝突させない）
  - **型の置き場所は 3 分類になった** — score / storage / コンパイル時のみ。
    `is_compile_time()` の 2 分法に `struct` は乗らない（実行時の型でレジスタに乗らないため）
  - 構築は定数部分を `set value` 1 コマンドで置き、実行時の値のフィールドだけ後から書く。
    プレースホルダを先に置くのはコマンド数が同じで、書き損じたときに静かに欠けなくなるため
  - **戻り値にはできない。** バニラの戻り値は整数 1 つで compound の戻る場所が無い。
    引数渡しと束縛どうしの複製は 1 コマンドでできる
  - `==` / `!=` も不可。実行時の compound どうしを比べる手段がバニラに無い
  - 自分を含む `struct` はエラー。有限の値を作れない
  - `if p { }` の曖昧さは Rust と同じ規則で解いた（条件とセレクタには構造体リテラルを書けない。
    括弧の中でなら書ける）
- [x] **M7-2** storage レイアウトとパス計算
  - `<namespace>:mw` の `mw.vars` 配下へのパス割り当て
  - 先に書くテスト: ネストした struct のフィールドパスが正しいこと
  - パスは束縛のパスに `.<フィールド名>` を継ぎ足すだけ。ネストしても規則は増えない
  - **代入の左辺は束縛から始まる名前の連なりだけ。** 式の結果のフィールドには書き込めない
    （書き込む先の名前が無く、storage にテンポラリを持っていないため）
  - フィールドの読み出しは**テンポラリを経由しない**。`execute store result score <dst>`
    が書き込み先を自分で持っているため（仕様 §6.4 と同じ理由）
  - **複合代入だけ 3 命令 = 5 コマンド。** 算術は score でしかできず、読み出しと書き戻しが
    どちらも `execute … run`（= 2 コマンド）になる。コマンド数は出力を読んで数えた
  - 例 `examples/mob-stats` を追加。struct の構築・受け渡し・フィールド読み書きが 1 つに載る
- [x] **M7-3** `#[nbt(...)]` 属性（§4.2）
  - タグ型指定 / `rename` / ~~`optional`~~
  - 先に書くテスト: `#[nbt(byte)]` を付けたフィールドが `Byte` として出力されること
  - タグは**型の一部**として持つ（`Field.tag`）。emit の都合ではない —
    バニラは違うタグで書いたデータを黙って無視するので、静的に決まっていないと検出できない
  - `i32` に付けられるのは整数タグだけ。**`bool` は `Byte` 固定** — 他の選択肢に意味が無い
  - rename 後のキーが衝突したらエラー。片方が黙って上書きされるため
  - **`optional` は先送り。** `Option<T>` として読む以上 `enum` が要る（M7-4・M7-5 の後）
  - `float` / `double` / `string` タグも先送り。対応する型が M8 で入る
- [x] **M7-4** `enum` とタグ付き union
  - 先に書くテスト: バリアントのペイロードが正しく読み書きされること
  - `{tag:"Chasing",target:3}`。ペイロードは**同じ compound に並べる**（要件定義 §4.2）
  - **タプル型バリアント（`Chasing(i32)`）は無い。** compound のキーは名前であって位置ではなく、
    `_0` のような綴りを発明しても読める出力にならない
  - `tag` という名前のフィールドはエラー。タグの置き場所と衝突する
  - **バリアントの切り替えは 1 コマンド。** `set value` が compound を丸ごと置き換えるので、
    前のバリアントのペイロードが残らない
  - 中身はフィールドアクセスでは読めない（どのバリアントかが静的に分からない）。`match` は M7-5
  - 型表は `Types`（struct と enum を 1 つの名前空間で持つ）に集約した。
    再帰検査も型グラフ全体で回すので、struct 経由の enum の自己参照も捕まる
- [x] **M7-5** `match`
  - `execute if data ... {tag:"X"}` の連鎖、網羅性検査
  - 先に書くテスト: 非網羅の `match` がエラーになること
  - **評価対象は判定の前に控える。** 腕はガードを順に評価するので、腕が評価対象を
    書き換えると（状態機械はまさにそれをする）次の腕も一致して二重に走る。
    実際にそのバグを踏んでからテストで固定した。控えは 1 コマンドで、これは削れない
  - 控え置き場として要件定義 §3.3 に `mw.tmp` を追加した（score の `$t<n>` の storage 版）。
    再帰時の退避対象にも入れてある
  - **`if` も `match` も式にしない。決定。** 合流点で値を作るには宛先駆動の lowering
    （積み残し）が要る。それまでは `let mut x = 0;` と各腕での代入が同じコマンド数で書ける。
    先に値を返す構文を入れると、テンポラリ経由のコピーが 1 つ増えたまま固定される
  - 腕の生成関数は `<親>/match_<n>/<バリアント名の小文字>`。データパックのパスが
    小文字しか受け付けないため。小文字化で衝突するバリアントの組はエラー
  - `_` は最後にだけ。全バリアントを挙げた後の `_` もエラー（到達しない腕を黙って受け取らない）
  - 例 `examples/turret` を追加。M7 の完了判定（struct と enum の状態機械）そのもの
- [x] **M7-6** `Vec<T>`
  - `len` / `push` / 定数添字 / 実行時添字（マクロ昇格）
  - 先に書くテスト: `v[i]` の実行時添字がマクロ関数を生成すること
  - **構築はリストリテラル `[1, 2, 3]`。** `Vec::new()` は入れていない — `[]` と同じものを
    2 通りで書けるようにしても増えるものが無い。空リストだけ注釈が要る（推論しないため）
  - `Type::Vec` は**要素型で intern**（`Type` が `Copy` なので中に型を持てない）
  - Place は「フィールドの列」から**ステップの列**（`Field` / `Index` / `At`）になった
  - **マクロ関数はここが初出。** 生成した補助関数の中だけに `$` の行があり、
    呼び出し側は素の関数のまま（要件定義 §10.1 の「伝染しない」）。
    渡す引数は**添字だけ** — 値は scoreboard 上にあり、フェイクプレイヤー名は静的に決まる
  - **実行時添字は最後のステップでだけ。** `v[i].x` はエラー。マクロ 1 回では書けず、
    テンポラリを増やして隠すより束縛に取り出させるほうが読める
  - 範囲外の添字は何も起きない（バニラの挙動）。検査コマンドを毎回吐くのは原則 3 に反する
- [x] **M7-7** `for x in vec`（破壊的反復、§7.1）
  - コピー作成 → `[0]` 消費のループ。マクロを使わないこと
  - 先に書くテスト: 生成物にマクロ行（`$` 始まり）が含まれないこと
  - 添字がつねに `[0]` なので**パスが静的に決まる**。これが破壊的反復を選ぶ理由そのもの
  - コピー置き場は `mw.iter.i<n>`。**元の `Vec` は変わらない**
  - `for` の構文はセレクタと共用。対象が `Vec` の束縛かフィールドなら list の反復、
    それ以外はセレクタ。`break` / `continue` / `return` は `while` と同じ扱い
  - 反復の束縛は `let` されたローカルと同じ。再帰時の退避対象にも入る
- [x] **M7-8** ジェネリクスの単相化
  - 型パラメータと ~~const パラメータ~~、インスタンスの一意化
  - 先に書くテスト: 同一型引数での重複インスタンス化が起きないこと
  - **型引数は書かない。引数の型から構造的に決まる。** turbofish は入れていない —
    決まらない書き方をエラーにするほうが、書き方を 2 つ用意するより読む側が楽
  - 実体は要求順に作り、実体の中からの要求も同じ列に積む（ワークリスト）。
    `FnId` は要求時に採番するので、`functions` の並びと id が一致し続ける
  - **実体の名前が register / storage の接頭辞になる**（`$first_i32.p`）。
    テンプレートの名前を使うと型違いの実体どうしが同じ場所を踏む
  - 型パラメータを持つフィールドのタグは**型引数から決まる**。`#[nbt(byte)]` を
    パラメータのフィールドに書くのはエラー
  - **const パラメータは M8 に送った。** 使い道（`fix<S>`）が M8 で入るまで検証できない
  - `enum` の型引数も同じ理由で先送り（`Option<T>` を入れるとき）
- [x] **M7-9** コンパイル時参照（§5）
  - `&` / `&mut`、`impl` の固有メソッド、呼び出し箇所ごとの単相化
  - 先に書くテスト: `&mut v[i]`（実行時添字）がエラーになること
  - **借用は名前の付け替え。** 実体ごとに借用元のパスを本体へ直接展開するので、
    引数の書き込みが 1 コマンドも出ない（要件定義 §5 のとおり）
  - `Place` に `Root`（`Local` / `Lent`）を足した。`Lent` は借用元の関数名と束縛名を持ち、
    パスもフェイクプレイヤー名も MIR 側で組み立てる（綴りの規則を 2 箇所に置かない）
  - **参照は引数にだけ書ける。** 束縛にすると生存期間の話になり、
    ライフタイムを持たない以上そこで嘘をつく。戻り値・フィールドも不可
  - メソッドは `<型>::<名前>` という名前の関数として持ち、パスは `<型>/<名前>` の
    サブディレクトリ。レシーバは第 1 引数
  - 借用のある関数は**呼び出し箇所ごとに実体化**する。同じ場所を借りる 2 つの呼び出しは
    同じ実体を共有する（表のキーに借用先を含めてある）
  - 例 `examples/turret` に `impl` を足した

---

## M8. 数値拡張

**完了の判定:** `fix<1000>` での座標計算が正しい値を出す。

- [x] **M8-1** `fix<S>` 型と const ジェネリクス
  - 先に書くテスト: `fix<100>` と `fix<1000>` の加算がエラーになること
  - **表現は `i32` と同じ。違うのは型だけで、その型がスケールを持つ**（仕様 §4.14・§6.25）。
    score 常駐なので MIR も emit も触っていない
  - **const 引数は `fix<S>` のスケールにしか書けない。** 型としても式の中の値としても
    使えない。値として読めるようにすると式に定数評価が要り、使い道がまだ無い
  - **const 引数は型引数の列に `Type::Fix(Scale::Const(n))` として乗る。**
    単相化の表もインスタンス名（`half_fix_1000`）も型引数と同じ仕組みで動く
  - `fix::<S>(e)` が唯一の作り方。`e: i32` は生の単位の読み替えで 0 コマンド、
    `e: fix<T>` は `* S` して `/ T`
  - **`*` と `/` の補正は HIR で `/ S` を継ぎ足す形にした**（`combine`）。
    MIR に fix を教える必要がなく、補正は既存の i32 の lowering で正しく落ちる
  - `fix<S> * i32` と `fix<S> / i32` は補正なしで許す。整数は倍率でありスケールを持たない。
    `+` で混ぜられないのは、足すべき値が `1` か `S` か字面から決まらないため
  - `a *= b`（両辺 fix）は `a = a * b` に書き換える。`operation` 1 つに補正が入らない
- [x] **M8-2** スケール補正の自動挿入
  - `*` / `/` でのスケール補正、リテラルの変換
  - 先に書くテスト: `fix<1000>` 同士の乗算が正しい値を出すこと
  - 補正そのものは M8-1 の型付けと同じ変更に入っている（HIR で `/ S` を継ぎ足す）。
    ここで足したのは**インタプリタで値を確かめる層**で、`0.5 * 0.5 = 0.25` は
    補正が無ければ 250000 になる
  - **どちらの向きも掛けてから割る。** 先に割ると、スケールが保とうとしている桁が消える
  - リテラルの変換は `fix::<S>(INT)` が 0 コマンドであることを指す。
    `fix::<100>(fix::<1000>(1500))` のように**両端が定数の変換はまだ 6 コマンド出る** —
    定数畳み込みは M9-5 の担当で、fix 専用の畳み込みを先に入れると二重になる
- [x] **M8-3** storage 専用型（`i8` / `i16` / `i64` / `f32` / `f64`）
  - 算術しようとしたら「`fix<N>` に変換しろ」と案内するエラー
  - 先に書くテスト: `f64 + f64` のエラーメッセージに変換の案内が含まれること
  - **型がタグそのもの。** だから `#[nbt(byte)] hp: f64` はエラーにした。
    タグを選びたいフィールドは `i32` か `fix<S>` で持つ（`#[nbt(short)] shots: i32`）
  - `is_storage()` を `is_compound()` と `is_nbt_scalar()` に割った。
    「storage にある」と「compound である」は M8 まで同じ意味だったが、もう違う
  - `NbtTag` に `Float` / `Double` を足した。SNBT の綴りは `1.0f` / `1.0d`
  - この時点では**値を作る手段が無い**（浮動小数点リテラルは無い）。
    構築も読み出しも M8-4 の往復が入って初めてできる
- [x] **M8-4** score ↔ storage の自動往復
  - `execute store` / `data modify ... from` の自動挿入、スケール指定
  - ~~先に書くテスト: エンティティの `Pos[0]` を `fix<1000>` に読み込めること~~ →
    **`f64` フィールドを `fix<1000>` へ読み、書き戻せること**に差し替えた。
    エンティティ NBT の読み取りは**失敗しうる**読み取りで、`Option<T>` を返す
    stdlib（要件定義 §9）と一緒に入るのが正しい。それは M9-1。
    構文を先に発明すると、`Option` が入ったときに 2 つ目の書き方が残る
  - 読みは `data get <path> S`、書きは `execute store ... <tag> 1/S`。**どちらも 1 コマンド**
  - `fix::<S>(場所)` が読み、`x.as_f64()` / `.as_i64()` などが書き。
    **受け手は場所でなければならない**（`data` コマンドのパスとして書かれるため）
  - **`fix<S>` から整数タグへの変換は入れていない。** 生の単位と丸めた値のどちらを
    書くのか字面から決まらない。`i32` を経由させる
  - tinymcf は scale 付きの `data get` も `execute store` も既にモデル化していた。
    インタプリタ側の変更はゼロ
- [x] **M8-5** `pos!` マクロ
  - 絶対 / `~` 相対 / `^` ローカル、混在の禁止規則
  - 先に書くテスト: `pos!(~ ^ ~)`（混在）がエラーになること
  - **記法と混在検査は M6 で入っていた**（コマンド引数に座標が要ったため）。
    ここで直したのは `~-1` が読めなかったことで、`~` `-` `1` の 3 トークンを
    1 つの座標として読むようにした。データパックがいちばん書くオフセット
  - **小数（`~0.5`）は入れない。** 字面としての小数は言語のどこにも無く、
    `pos!` にだけ用意すると数の書き方が 2 つになる（仕様 §3.9）
- [x] **M8-6** `String` の操作（§4.4）
  - `len` / 定数 `slice` / リテラル比較（無料）、連結 / 実行時比較（マクロ昇格）
  - 先に書くテスト: `s.len()` がマクロを生成しないこと
  - **比較は照合。** バニラに文字列を比べるコマンドは無く、「このパスはこの値を持つか」
    しかない。だから親のパスへの `{キー:"lit"}` 照合になる。
    リストの要素のようにキーで終わらないパスは `mw.tmp` の一時に写してから照合する
  - **相手が実行時の文字列ならマクロ昇格。** 連結も同じ。ただし**リテラルの部分は
    マクロの値に直接埋め込む**ので、`a + "cd"` の引数は 1 つ。両辺リテラルなら
    マクロ自体が消える（設計原則 1）
  - `slice` のために**範囲式**を足した（仕様 §3.17）。`Range` 型は無く、
    `slice` の引数以外に書くとエラー。境界はコンパイル時定数
  - `is_storage()` の下に `is_storage_scalar()`（NBT スカラ + `String`）を置いた。
    compound ではないが storage にある、という 3 つ目の区別
  - **一時のパスは `data_temp()` から採る。** 固定パスを書くと再帰の退避対象から漏れる
    （引き継ぎに書いてあった罠そのもの）
- [x] **M8-7** `nbt!` マクロと struct 型の照合
  - 先に書くテスト: 型に無いフィールドを含む `nbt!` がエラーになること
  - **キーは NBT 上の名前**（`#[nbt(rename)]` 後）。足りないキーもエラーにした —
    値は丸ごと 1 つの `set value` になるので、欠けると型が言っていない状態ができる
  - **接尾辞は書かせない**（`20f` ではなく `20`）。フィールドの型がタグを決めており、
    両方に書かせると食い違える。要件定義 §10.2 の例は `20f` だが、
    M8-3 で「型がタグそのもの」を決めた以上こちらが一貫する
  - **小数も書けない。** `2d` は字句解析器が「10 進整数ではない」と言う。
    浮動小数点リテラルが言語のどこにも無いのだから、ここにだけ作らない
  - 書き込み先の型が分かる位置は今のところ**注釈付きの `let` だけ**。
    引数位置での照合は、宛先駆動の lowering（M9-10）で式の期待型を配ったときに一緒に入る

---

## M9. 仕上げ

**完了の判定:** `mwl new` から `mwl install` まで一連の操作が通り、release ビルドが最適化される。

- [x] **M9-1** `Option<T>` と `?` / `if let` / `match`
  - `execute store success` による失敗検出
  - 先に書くテスト: 存在しない NBT パスの取得が `None` になること
  - **`Some` / `None` はパスの有無で表す。** タグ付き compound（`{tag:"Some"}`）にすると、
    バニラが書いた NBT の「欠けているフィールド」を `None` として読めなくなる（仕様 §4.19）
  - その結果 **`enum` のジェネリクスは要らなくなった。** `Option<T>` は `Vec<T>` と同じく
    中身の型で内部化される組み込み型で、ユーザが `enum` に型引数を書く手段は今も無い
  - **`#[nbt(optional)]` は入れず、`hp: Option<i32>` と型に書かせる**（要件定義 §4.2 からの逸脱）。
    属性と型の両方に書かせると食い違える。M8-3 の「型がタグそのもの」と同じ判断
  - **`?` はレジスタに開ける**ので中身は score に載る型に限る。compound は `match` で開ける
  - **`match` / `if let` は場所でなくてよい。** `Option` を返す呼び出しは
    成功フラグと結果の 2 つで答えるので、compound を照合する `enum` と違って
    storage に置く必要が無い
  - 関数から返す `Option<T>` は `return <値>` と `return fail`。呼び出し側は
    `execute store success ... store result ... run function` の 1 コマンドで両方受け取る
  - 制御レジスタに `4`（失敗して抜けた）を足した。`3`（値を返した）と同じ経路で伝播する
  - **tinymcf: 無いパスの読みは診断を出さないことにした**（SPEC §4.2）。
    未設定の score が条件を偽にするのと同じで、これが無いと `Option` を使うたびに
    「失敗した」と言われる。**複数一致は今までどおり診断**
- [x] **M9-2** `debug_assert!` / `expect()`
  - debug で `tellraw` 報告、release で消去
  - 先に書くテスト: release 出力に assert 由来のコマンドが無いこと
  - **release では文ごと落とす。** 出力から命令を消すのではなく MIR に降ろさないので、
    条件の評価もテンポラリの確保も起きない。テストは「grep して無い」ではなく
    **コマンド数が変わらないこと**で確かめている
  - `debug_assert!` は**パース時に式として読む**（`raw!` のようなトークン列ではない）。
    言語で書かれた検査なので、型検査もそのまま効く
  - 報告の場所（`test.mwl:1`）は emit が span から作る。行番号コメントと同じ仕組みで、
    ソース文字列を持っているのが emit だけだから
  - `expect` は `?` から早期 return を抜いたもの。**止まらない** — バニラに中断は無く、
    報告して値はそのまま流れる
  - `mir::lower` が profile（`debug: bool`）を取るようになった。emit の `Profile` を
    mir から参照すると依存が逆向きになるので bool で渡している
- [x] **M9-3** `raw!` のマクロ昇格（§3.21・§6.31）
  - 定数補間は無料、実行時補間は**その行だけを本体に持つ補助関数**へ落とす。
    `mw.args.a<n>` に書いてから `function <親>/raw_<n> with storage`。
    §6.21 の実行時添字・§6.27 の連結と同じ 3 点セット
    （`macro_helper` / `Inst::Macro` / `Inst::CallWithArgs`）で書けた
  - **`#[tick]` / `#[load]` の検査は入れていない。** 当初の計画は
    「関数そのものをマクロ関数へ昇格させ、タグの関数が昇格したらエラー」だったが、
    その案は**局所変数を補間できない** — `let x = f();` の `x` は呼び出し側が知らないので
    `mw.args` に書ける者がいない。補間できるのが仮引数だけになる。
    補助関数に閉じれば `$` の行は生成関数の中だけに現れ、タグの関数は昇格しない。
    **要件定義 §10.1 を更新済み**（採らなかった案とその理由も残した）
  - 先に書くテスト: `#[tick]` 関数の実行時補間が診断なしで動くこと（昇格が伝染しない）
  - **`{` はつねに補間の始まり。** `{{` / `}}` で文字そのもの。
    生コマンドは NBT の `{...}` を書くことが多く、「解決できなければ文字面」に逃がすと
    綴り違いが黙って通る（設計原則 2）。`examples/burn-the-undead` を二重に直した
  - 補間できるのは `Selector`（無料）と `i32` / `bool` / `String`。
    **`fix<S>` は拒否する** — score にあるのはスケール後の整数で、字面が値と食い違う
  - コストは実行時の値 1 つにつき 4 コマンド（マーシャルの `execute store result … run` が
    2、呼び出し 1、マクロ行 1）。生成物を読んでから書いた
- [x] **M9-4** `text!` マクロ（§3.22・§6.32）
  - 連結、自動変換、コンパイル時メソッドチェーンによる装飾。**コマンドは 1 つ、
    実行時コストはゼロ** — JSON が score と storage を直接指せるので、`raw!` と違って
    マクロ関数が要らない
  - 先に書くテスト: `text!("HP: ", hp)` の JSON が score コンポーネントを含むこと
  - **`text!` は parser が式として読む。** `debug_assert!` と同じ扱いで、
    トークン列ではない。だからメソッドチェーン（`"a".red().bold()`）が素の構文で通り、
    HIR がトークンを手で解析する必要が無い
  - **連結は `{"text":"","extra":[…]}`。** 先頭を空にしないと、リストの先頭要素の装飾を
    後続が受け継いで `text!("a".red(), "b")` の `"b"` まで赤くなる
  - **`names` モジュールを足した**（`crates/mwlc/src/names.rs`）。フェイクプレイヤー名・
    `mw.vars` のパス・objective・storage の id を 1 か所で決め、mir と emit と HIR が
    そこを見る。**JSON の中の名前が実際の出力とずれると、バニラは黙って何も表示しない** —
    ちょうどこのコンパイラが防ぎたい種類の失敗なので、定義を分けない
  - **書けるのは束縛そのものだけ。** `m.hp` は先に `let` で受ける。場所のパスを組み立てるのは
    MIR の仕事で、`text!` は HIR で閉じている
  - **`fix<S>` は拒否する。** バニラの score コンポーネントに倍率が無く、
    スケール後の整数がそのまま出る（M9-3 の `raw!` と同じ判断）
  - **`.on_click` / `.on_hover` / `translate!` は入れていない。** クリックとホバーの JSON は
    1.21.5 で形が変わっており、**版差は toolchain が持つもの**（AGENTS.md）。
    toolchain がイベントの形を記述できるようになってから。**要件定義 §10.3 に明記済み**
  - 装飾メソッド名（`.red()` や `.bold()`）は予約。同名の固有メソッドは書けない
  - `examples/arena` の手書き JSON の `tellraw` を `text!` に置き換えた
- [x] **M9-5** release 最適化 1 — 定数畳み込み（§6.33）
  - 先に書くテスト: 畳み込み前後で `tinymcf` の実行結果が一致すること
  - **MIR の `Value::Const` の延長で済んだ。** 定数は最後まで materialise されないので、
    両端が `Value::Const` の二項演算を計算して `Value::Const` を返すだけ。
    テンポラリも `Inst` も生まれず、`fold` は 30 行
  - **debug では畳み込まない**（要件定義 §15: ソースと出力が 1:1）。
    `mir::lower` が既に持っている `debug` フラグで切る
  - **バニラの算術に合わせる。** 加減乗は wrapping、除と剰余は**切り下げ**
    （`-7 / 2` は `-4`、`-7 % 2` は `1`）、`&&` / `||` は 0/1 の min / max。
    `floor_div` は `tinymcf` と同じものを書き写した — **`mwlc` は `tinymcf` に
    依存できない**ので共有はしない
  - **0 除算は畳み込まない。** バニラは失敗して対象を変えずに残す。答えを決めてしまうと
    その挙動が消える。回帰テストを 1 本置いた
  - 効果: `let a = 2 + 3 * 4; let b = fix::<100>(fix::<1000>(1500));` が
    **13 コマンド → 2 コマンド**。M8-2 が積み残した「両端が定数の fix 変換で 6 コマンド」も
    これで消えた
- [x] **M9-6** release 最適化 2 — デッドコード除去（§6.34）
  - 未到達関数、未使用 objective
  - 先に書くテスト: 未呼び出し関数がファイルとして出力されないこと
  - **根は function タグの関数だけ。** バニラがパックに入る道は function タグしかない。
    到達判定は MIR の `Call` / `CallWithArgs` を辿る幅優先で、`mir::lower` の末尾で行う
  - **`raw!` の中で名前が出る関数も根にする。** `raw!` はコンパイラが読まないテキストで、
    そこに `function myns:foo` と書ける。**多く残すのはファイル 1 つの無駄、
    消し過ぎは実行時の沈黙**なので、迷ったら残す
  - objective は**生成済みのコマンド文字列**を見て、名前が出るものだけ作る。
    MIR を見ないのは、`raw!` が objective を直に書く場合を拾えるのがテキスト側だけだから
  - **`Mir` に `namespace` を持たせた。** 今まで emit が関数パスから読んでいたが、
    全部落ちると読む先が無くなり `data/minecraft/...` に書きに行っていた
  - **副作用: release では `#[tick]` / `#[load]` の付かない関数が消える。**
    `/function` を手で打って呼ぶつもりの関数を宣言する手段が v1 に無いため
    （§3.12 の `pub` / `extern fn` は未定）。既存テストの入口に属性を足して回った。
    **仕様 §6.34 に明記し、積み残しに入れた**
- [x] **M9-7** release 最適化 3 — レジスタ再利用（§6.35）
  - 先に書くテスト: 逐次的な式 10 個でテンポラリが 10 個未満に収まること
  - **生存解析は要らなかった。** テンポラリは文をまたいで生きない — 文は順に走り、
    生成関数は次の文が始まる前に返っている。だから**文の切れ目で番号を巻き戻す**だけ。
    巻き戻す先は文の並びに入る直前の値で、`for` のリストの控えや `while` の条件は
    それより前に採られているから本体が踏むことはない
  - **コマンド数は減らない。** 減るのは `<ns>.t` のフェイクプレイヤー数で、
    要件定義 §15 が挙げている肥大化がそれ
  - **M8-6 で見つけた無駄を直した。** 正体はテンポラリではなく**ブロック内の束縛**で、
    `if` の中でだけ使う束縛が**その `if` を通らなかった経路でも退避対象に入り**、
    `scoreboard players get` が失敗していた。`f(4)` の例で
    **94 コマンド・失敗 3 件 → 82 コマンド・失敗 0 件**
  - **退避リストの絞り込みは両方のプロファイルで行う。** 失敗するコマンドを出さないのは
    最適化ではなく修正だから。名前の使い回しだけが release 限定
  - 回帰テスト: 再帰・ループ・`match` を混ぜたプログラムで debug と release の
    実行結果が一致すること
- [x] **M9-10** 宛先駆動の式 lowering（§6.37）
  - 式の結果をテンポラリ経由でコピーせず、宛先へ直接書く
  - **書ける形は 2 つだけに絞った。** 呼び出しそのものと、定数と束縛だけでできた
    算術の連なり。`&&` / `||` も 0/1 の min / max なので同じ形に乗る
  - **`let` の新規束縛だけが宛先。** そこはまだ誰も読んでおらず、退避リストにも無い
  - **右側に呼び出しがあるときは従来どおり。** `f(a) + g(b)` で `g` が再帰すると、
    宛先の `f` の答えを呼び先が同名レジスタとして潰す
  - **宛先が被演算子に現れる形も従来どおり。** 同じ名前の束縛は同じレジスタなので、
    `let x = x + 1;` は先に宛先を書くと読む値が消える
  - 効果: 呼び出し 1 つにつき 1 コマンド、算術の連なり 1 段につき 1 コマンド
  - **期待型を引数にも配った。** `f(nbt!({ .. }))` が書けるようになった。
    型引数のままの仮引数では照合先が決まらないので拒否する
  - **作業中に既存のバグを 1 つ見つけた**（別コミット）。呼び出しの答えを入れる
    テンポラリが `used` に積まれておらず、**同じ式の 2 つ目の呼び出しで潰れていた**。
    `fib(n-1) + fib(n-2)` が誤った値を返す。`factorial` は 1 文 1 呼び出しなので
    通っていた
- [x] **M9-11** エンティティ NBT のビュー（§3.19・§4.20・§6.29）
  - `#[entity] struct` と `T::of(セレクタ)`、読み書き、単一対象の検査
  - 先に書くテスト: 複数一致しうるセレクタ（`@e[type=zombie]`）がコンパイルエラーになること
  - **ビューは値ではない。** `Type::View` はコンパイル時のみの型で、束縛は場所の別名
    （借用と同じ `place_aliases` に乗る）。`let` 自体は文にならない
  - **データ命令に `DataRef`（対象 + パス）を持たせた**のが作業の本体。
    `storage <ns>:mw <パス>` と `entity <セレクタ> <パス>` の違いはここだけに閉じている
  - **実装して初めて分かったこと: スケールはタグが決める。**
    `fix<S>` が float / double タグなら実数、整数タグなら生の単位。
    最初は「ビューだから実数」と考えたが、それだと同じ型が置き場所で意味を変える。
    タグで決めれば storage でも entity でも同じ規則になる（仕様 §4.16 に追記）
  - コンパイル時の型（`Selector` / `Pos` / ビュー）を**引数とフィールドに書けない**検査を足した。
    今まで書けてしまい、MIR で `unreachable!` に落ちていた
  - **ブロックは入れていない。** 座標モデルが要り、`positioned` / `if block` を
    先送りしているのと同じ理由
  - `examples/arena` がビューを使うようになり、`merge_entity` もインタプリタで動く
- [x] **M9-8** コマンド数の静的検査と `cost.txt`（§6.36）
  - ループなしは正確に計算、ありは 1 周あたりのコスト。`tinymcf` の実測と突き合わせ
  - 先に書くテスト: 静的計算値と `tinymcf` の実測値が、ループ無しケースで完全一致すること。
    `examples/arena` の `setup` でも一致した（どちらも 35）
  - **1 命令 1 コマンドという建て付けがそのまま効いた。** `inst_cost` は
    `emit::command` の入れ子と 1:1 で、`execute … run` が 2、`$` のマクロ行が 1
  - **ガードは通る前提で数える。** `maxCommandChainLength` が気にするのは最悪値
  - **`+` は「1 周分」の印。** ループ・再帰に加えて、**複数一致しうるセレクタの
    `execute as`** も対象 — 見落とすと `for z in @e[...]` が 1 回分に見える
  - 循環はメモ化せずに歩く。上から来た経路で答えが変わるので、メモ化すると
    次の呼び出し元に嘘をつく
  - `Datapack` に `costs` を持たせた。ファイルではないので `write_to` は触らず、
    `mwl build` が `target/cost.txt` に書いて、閾値超過を stderr に警告する
- [x] **M9-9** CLI 完成
  - `mwl new` / `check` / `test` / `install`
  - 先に書くテスト: `mwl new` が生成したプロジェクトがそのまま `mwl build` で通ること。
    加えてテンプレートが自分の `#[test]` に通ること
  - **`mwl test` のために `#[test]` を足した**（§3.23）。引数も戻り値も `#[ctx]` も取れない。
    失敗の定義は「`debug_assert!` の報告が出たか、インタプリタが診断を出したか」で、
    **バニラに実行の中断が無い以上、テストは止まらず走り切って報告だけ残す**
  - **テストごとにインタプリタを作り直す。** 前のテストが残したスコアで次の合否が
    変わってはならない
  - **テストは release に出力されない。** function タグから辿れないので M9-6 が落とす。
    配布物にテストコードが入らないのは意図した結果
  - `mwl` が `tinymcf` に依存する。**`mwlc` は依存しない**ままなので不変条件は保たれる
  - `mwl new` は名前を namespace として検査する（大文字はデータパックのパスに使えない）
  - `new_project_in` / `run_tests_in` と場所を引数で受ける形にした。
    テストがプロセスの作業ディレクトリを動かさずに済む（AGENTS.md の罠）

---

## M10 座標とブロック（v1 の後）

**v1 の計画はすべて閉じた。** ここからは要件定義 §4.2 が「座標のモデルが要る」として
外していたものを開ける。要件定義 §6 は実行コンテキストに位置と回転があると書いていて、
`Pos`（要件定義 §5）も絶対・`~`・`^` の 3 記法を持っている。**足りないのは実装だけ。**

**実行時の座標は作らない。** バニラの raycast は座標を計算せず、
`execute positioned ^ ^ ^0.1` を再帰で積む。だから座標はコンパイル時のままでよく、
設計原則 1（静的に分かるものは無料）がそのまま通る。

- [x] **M10-1** `tinymcf`: 座標と `positioned`（SPEC §4.4）
  - 絶対 / `~` / `^` の 3 記法。コンテキストに回転を足し、`at` が位置と回転の両方を移す
    （バニラの `execute at` は位置・回転・次元を移す）
  - 先に書くテスト: `at @s` のあと `positioned ^ ^ ^1` が向いている方向へ 1 進むこと
  - **`Effect` に位置を持たせた。** 引数は書かれたまま（`~ ~1 ~`）記録されるので、
    解決後の座標が出てくる場所が他に無い
  - `^` は 3 つとも `^` でなければエラー（バニラと同じ）。`~` と `^` 単体は「ずらさない」
  - `rotated <yaw> <pitch>` も入れた。`positioned as` / `rotated as` は
    まだ誰も要らないので保留のまま
- [x] **M10-2** `tinymcf`: ブロックと `if block`（SPEC §4.4・§4.6）
  - 世界にブロックを持たせる。`setblock` が実際に書き、`if block` が読む。
    ブロックの NBT は `data ... block <x> <y> <z> <path>`
  - 先に書くテスト: `setblock` した座標を `if block` が見つけること
  - **`setblock` だけは記録するだけでなく適用する。** そうしないと `if block` が
    ハーネスの置いたものしか見えず、**パックが自分で建てたものをテストできない**。
    ブロック状態や `destroy`/`keep` は適用しない — 誰も要求していない形を
    当て推量するのは、知らないままでいるより悪い
  - **無いブロックは air。** 条件は偽になるだけでエラーにしない（未設定のスコアと同じ）。
    ただし**air への NBT 書き込みは失敗して理由を言う**（バニラと同じ）
  - `stone` と `minecraft:stone` は同じブロック。位置は**内側のブロックに落ちる**
    （バニラは floor する）
- [x] **M10-3** 言語: `positioned` 文（§3.24・§6.38）
  - `positioned pos!(~ ~1 ~) { .. }`。`#[ctx(position)]` を満たす 2 つ目の手段になる
  - 先に書くテスト: `positioned` の中で `#[ctx(position)]` の関数を呼べること
  - **`if` のブロックと同じ形に落とした。** 1 回しか走らないので、`as` / `at` / `for` の
    「エンティティごと」の仕掛け（`break` の伝播、実行者ガード）は要らない
  - **位置だけを与える。** 座標の後ろにエンティティはいないので `@s` は外側のまま。
    `positioned` の中から `#[ctx(entity)]` を呼ぶのはエラーのまま
  - キーワードを 1 つ増やした（仕様 §2.4）。`at` に座標も取らせる案は採らなかった —
    バニラの `at` は回転も動かし、`positioned` は動かさない。**観測できる違いがある**
  - tree-sitter と TextMate の両方に足した
- [x] **M10-4** 言語: `if block(...)` 条件（§6.39）
  - `execute if block` はそれ自体が条件なので、レジスタを経由しない
  - 先に書くテスト: `if block(pos!(~ ~-1 ~), minecraft:stone)` が 1 コマンドで済むこと
  - **組み込み関数にした。** 世界に**尋ねる**もので、世界に対して実行するものではないので、
    コマンド表からは引かない。`fn block` を定義すればユーザ側が勝つのは他と同じ
  - 引数はどちらもコンパイル時の型なので、組み立てにコストがかからない
  - 値としても受けられる（`execute store success` で 1 コマンド）
  - ブロック状態（`minecraft:chest[facing=north]`）はまだ書けない
- [x] **M10-5** 言語: ブロック NBT のビュー（§3.25・§6.40）
  - `#[block] struct` と `T::of(pos!(..))`。M9-11 のエンティティビューと同じ仕組みで、
    `DataRef` の対象が増えるだけ
  - 先に書くテスト: ブロックのフィールドが読み書きできること
  - **`at` ではなく `of` に寄せた。** `at` はキーワードなので `Chest::at(..)` が
    字句解析を通らない。`of` 1 つにして、**何を取るかは属性が決める**
  - **`~` / `^` を書いたら `#[ctx(position)]` を要求する。** 相対座標はコマンドが走る
    場所で読まれるので、位置が無ければ意味が決まらない
  - **M9-11 が `DataRef` に違いを閉じ込めておいたのが効いた。** 3 つ目の対象を足すのに
    触ったのは `place_path` と emit の 1 行ずつ
- [x] **M10-6** raycast の例
  - `examples/raycast`。`positioned ^ ^ ^1` の再帰と `if block` で書く
  - 先に書くテスト: `tinymcf` で当たり判定が出ること（X-4 のベンチマークにも入れた）
  - **座標を 1 度も計算しない。** `^ ^ ^1` が 1 歩で、ループは再帰。実行時の値は
    「残り歩数」だけ。**`Pos` に実行時表現が要らない**ことがこれで裏付けられた
  - 当たったブロックの NBT を `#[block]` ビューで読むところまで入っている
  - **作業中に既存のバグを 1 つ見つけた**（別コミット）。1 文のブロックを inline に
    試して**諦めたとき、その試行が取ったテンポラリが退避リストに残っていた**。
    後続の再帰呼び出しが「誰も書いていないレジスタ」を退避しようとして、
    呼び出しごとに `scoreboard players get` が失敗する。`if` と `at` にも同じ穴があった

---

## M11 ブロック状態・可視性・LSP（v0.1.0 の後）

v0.1.0 を公開したあとの 3 つ。**順に独立**なので、どれから入ってもよい。

- [x] **M11-1** ブロック状態（§2.8・§6.39、SPEC §4.4）
  - `block(pos!(~ ~ ~), minecraft:chest[facing=north])` と `setblock` の状態
  - **バニラは部分一致**: 書いた状態だけを見て、書かなかった状態は問わない。
    これは述語であって値ではないので、その意味をそのまま写す
  - 字句は**セレクタと同じ手**でいける — リソースロケーションの後ろに括弧の対応が
    取れた列を 1 トークンとして足す。リソースロケーションは添字を取らないので曖昧さは無い
  - HIR より下は**何も変わらない**（`ExprKind::Resource` の文字列が長くなるだけ）
  - 先に書くテスト: `chest[facing=north]` が `facing=south` の chest に一致しないこと、
    かつ状態を書かない `chest` はどちらにも一致すること
  - **コンパイラ側は字句 1 行で済んだ。** リソースロケーションの後ろの `[..]` を
    セレクタと同じ `balanced()` で 1 トークンに含めるだけ。HIR より下は無変更
  - `setblock` も状態を持つようになった（コマンド中の NBT はまだ記録のみ）
- [x] **M11-2** 外から呼ぶ関数の宣言（`pub`）（§3.26）
  - release の DCE が function タグから辿れない関数を落とす（§6.34）。
    `/function` を手で打つ・他のパックから呼ぶ関数を残す手段が無い
  - `pub fn` を DCE の根にする。予約語は確保済み（仕様 §2.4）
  - 先に書くテスト: `pub fn` が release ビルドに残り、付いていない関数は落ちること
  - **意味は 1 つだけ: 「パックの外から呼ばれうる」。** モジュールの可視性ではない
    （`mod` / `use` はまだ無く、隠す相手がいない）
  - **`#[ctx]` は拒否しない。** タグと違い、外からの呼び出しが実行者を持っていることは
    ありうる（コマンドブロックの `execute as ... run function`）
  - `pub` から辿れる関数も一緒に残る（根が 1 つ増えるだけ）
- [x] **M11-3** LSP（`crates/mwl/src/lsp.rs`）
  - 保存時の診断が最初。`mwlc` は既に span 付きの `Report` を返すので薄い。
    定義ジャンプと補完はその次
  - **コンパイラを lib として叩く**。エディタのプラグイン（X-6）はハイライトだけを
    持ち、コンパイラに依存しないままにする
  - 先に書くテスト: 壊れた `.mwl` を開くと診断が 1 つ返ること
  - **`mwl lsp` サブコマンド。** 新しい crate は作らない。エディタは 1 つのバイナリを
    指すだけで済む
  - **プロトコルは手で書いた。** `Content-Length` の枠と JSON-RPC しか要らず、
    依存は `serde_json`（既に木の中にある）だけ。**コンパイラに非同期ランタイムを
    生やす場所ではない** — `mwl toolchain install` で TLS を足さなかったのと同じ判断
  - 同期は full text。`.mwl` は小さくコンパイラは速いので、差分を追う利点が無い
  - 近くの `minewell.toml` から namespace と toolchain を決める。
    **コマンド表はキャッシュする**（1 打鍵ごとに 400KB の JSON は読まない）
  - 位置は **UTF-16 コード単位**で数える（LSP がそう定義している）。
    字句は ASCII だけだが、文字列とコメントには何でも入る
  - 統合テストはバイナリを起動して実際にパイプで喋らせる

---

## 横断タスク

- [x] **X-1** CI（`cargo test` / `clippy` / `fmt`）
  - `.github/workflows/ci.yml`。mise-action + `Swatinem/rust-cache`
  - ガードは M0-1 で除去済み
- [x] **X-2** toolchain 生成 CI
  - `server.jar` の data generator を実行して Releases へ公開
  - `scripts/build-toolchain.sh <version>` が本体。マニフェストから server.jar を引き、
    `--reports` を走らせ、`commands.json` と `toolchain.json` を
    `mwlc::toolchain` が読む形で置く。**pack_format は jar の中の `version.json`** から採る
  - `.github/workflows/toolchain.yml` は **`workflow_dispatch` のみ**。
    server.jar のダウンロードとリリース作成が push で起きてよいことではない。
    入力はシェルに貼らず環境変数経由で渡す
  - **手元で 1.21.4 を実際に生成して検証した。** `pack_format` は 61、
    `commands.json` は 400KB。**`examples/arena` が実物のコマンド表でそのまま通り**、
    出力は手書き toolchain と 1 バイトも変わらなかった（`playsound <sound> master <targets>`
    の語順も含めて）。M6 の設計が実データで裏付けられたのはこれが初めて
  - **最初のリリース（`toolchain-1.21.4`）を切って `mwl toolchain install` を実装した。**
    取得は `curl` と `tar` に任せる — 書庫はこのスクリプトが `tar -czf` で作ったものなので、
    展開は同じ操作の逆で、**コンパイラに TLS スタックを生やす場所ではない**。
    どちらも macOS / Linux / Windows 10 以降に付いてくる。無い環境には `add` がある
  - 入れた直後に `load` して**中身を確かめる**。読めない toolchain は、無いものより悪い
    （後で、別の場所で失敗する）
  - テストは `file://` から取る。**GitHub が落ちていると落ちるテストは、
    間違った理由で落ちるテスト**
- [x] **X-3a** `tinymcf` の対象サブセット仕様 `crates/tinymcf/SPEC.md`
  - モデル化する mcfunction の範囲、バニラとの意図的な差異、失敗モデル、決定性の保証
  - **`docs/` ではなく crate 同梱。** `tinymcf` は独立公開するので契約もコードと一緒に動く
  - 各項目に done / pending と対応タスク番号を持たせ、M0 の残りはここを先に更新してから実装する
- [x] **X-3b** minewell 言語の詳細仕様書 `docs/02-spec.md`
  - 初版は M3 で用意し、以降マイルストーンごとに該当節を先に確定させて運用している
  - M7 終了時点で §2〜§6 は `struct` / `enum` / `match` / `Vec` / ジェネリクス / 参照まで確定
  - **実装が仕様に追いつくのではなく、仕様が実装を先導する。** この運用自体は M8 以降も続く
- [x] **X-4** ベンチマーク
  - 代表的なデータパックの生成コマンド数を追跡。`crates/mwlc/tests/benchmarks.rs`
  - **`insta` のスナップショットに `cost.txt` の表そのものを撮る。** 追跡の仕組みを
    別に作らず、M9-8 の静的コスト表を release ビルドで撮るだけにした。
    lowering を変えると差分が出るので、**レビューがそのまま追跡になる**
  - 題材は状態機械（`enum` + `match`）・インベントリ（`Vec` と `for`）・再帰の 3 つ。
    **raycast は書けない**（座標とブロックのモデルが要り、`positioned` / `if block` は
    先送り中）ので入れていない
- [x] **X-6** エディタの構文サポート（LSP ではない）
  - `.mwl` の tree-sitter grammar を 1 つ書き、nvim と zed はそれを指す。
    VS Code は TextMate grammar が要るので、同じ字句規則から 2 本目を書いた
  - **要件定義 §19 が外しているのは LSP であって、ハイライトではない。**
    ハイライトはコンパイラに一切依存しないので、v1 の中に置ける
  - 先に書くテスト: grammar が `examples/` の全 `.mwl` をエラーなく解析すること。
    `mise run grammar` がそれで、**`ci` に入れた**
  - 置き場所は `editors/{tree-sitter-mwl,vscode,zed,nvim}`
  - **生成物（`src/parser.c`）はコミットしない。** 580KB の生成 C を、出力の読みやすさを
    掲げるリポジトリに置きたくない。作るのは `mise run grammar` の 1 コマンド
  - ハイライトクエリは 1 本だけ持ち、**zed からはシンボリックリンクで指す**。
    2 本置くと必ずずれる
  - 罠は仕様どおり 3 つとも踏んだ: リソースロケーション（1 トークン、`.` と `-` 込み）、
    セレクタ（1 トークン、文字列中の `]` で閉じない）、`fix::<1000>` のターボフィッシュ
  - **`if x { }` と `Point { .. }` の曖昧さが本番だった。** 実際のパーサは
    `no_struct_lit` フラグで解くが、tree-sitter では `conflicts` に登録して
    両方の読みを並べ、`prec.dynamic` でブロック側を勝たせた
  - 型名は**ノードで包まずに `alias`** する。包むと「識別子か型名か」を
    `::` を見る前に決めさせられて、`Threat::Calm` が読めない
- [x] **X-5** README
  - README は M0 の途中で先行して用意
  - `examples/` に 4 本（hello / burn-the-undead / countdown / recursion）。
    M5 完了を待ったのは、それまでだと `raw!` だらけの見本になるため
  - `tests/examples.rs` が全 example を debug と release の両方でビルドする。
    コンパイルしなくなった見本は、無いより悪い

---

## クリティカルパスと順序の根拠

```
M0 ──> M1 ──> M2 ──> M3 ──> M4 ──> M5 ★ ──> M6 ──> M7 ──> M8 ──> M9
             (X-1)                          (X-2)              (X-4)
                      (X-3 初版)      (X-5)
```

- **M0 が先頭** — 測定器なしの TDD は golden file 化して後で全書き直しになる
- **M5 が M6 より前** — `#[ctx]` 検査が動いた瞬間に minewell は使う価値のある道具になる。
  逆順にすると「機能は多いが誰も嬉しくない」状態が長く続く
- **M9 の最適化が最後** — `tinymcf` の計測（M0-11）と回帰テスト（M1-9）が揃って
  初めて「意味論を変えずに縮んだ」ことを証明できる。それ以前に入れると検証コストだけ払う
- **X-3（仕様書）は並走** — 仕様書駆動なので、各マイルストーンの該当節を実装前に書く

---

## 保留・将来（v1 スコープ外）

要件定義 §19 の項目。**着手しない。** 予約した識別子だけ確保しておく。

`async` / `await`・`trait` / `dyn`・`Result`・ユーザ定義マクロ・借用チェッカ・
advancement / predicate 生成・LSP・パッケージレジストリ・動的ディスパッチ・分割コンパイル
